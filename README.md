## Overview

A simple example to evaluate how leveraging Electron and WebdriverIO together.

### Steps to replicate this project

1. Install all the depedencies `npm install`
2. Run all test `npm test`
3. Open app under debugging mode `npm start`
